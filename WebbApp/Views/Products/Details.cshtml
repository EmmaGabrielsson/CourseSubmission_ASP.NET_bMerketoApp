@model string 
@{
    var product = await productService.GetAsync(x => x.ArticleNumber == Model);
    GridCollectionItemViewModel Modell = new()
    {
                ImageUrl = "wwwroot/images/placeholders/501x430.svg",
                Title = "Young Star Smart Shoe in Fashion",
                Description = "Lirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua. At vero eos et accusam et justo duo dolores et ea rebum. Stet clita kasd gubergren, no sea takimata sanctus est Lorem ipsum dolor sit amet.",
        Price = 30,
        Categories = new List<string>{"Table Lamp", "Light", "Electronic"}
    };
}
<section class="breadcrumb">
    <div class="container">
        <div class="content text-center text-dark">
            <h2>SHOP</h2>
            <ul>
                <li>HOME</li>
                <li>PRODUCTS</li>
                <li>DETAILS</li>
            </ul>
        </div>
    </div>
</section>

<section class="details-container">
    <div class="image-container">
        <img src="@product.ImageUrl" alt="@product.ProductName"/>
    </div>
    <div class="product-details">
        <h4 class="title">@product.ProductName</h4>
        <p class="product-description">@product.Ingress</p>
        <div>
            <p>Rating: ***** <span>Reviews </span>
            </p>
        </div>
        <p class="product-price">$@product.Price</p>
        <div class="button-container">
            <div class="quantity-container">
                <button class="btn-outline-white" type="button">-</button>
                <input type="number"  min="1" />
                <button class="btn-outline-white" type="button">+</button>
            </div>
            <button class="btn-theme add-to-cart">
                Add to Cart 
                <i class="add-cart fa-regular fa-cart-plus"></i>
            </button>
        </div>
        <p class="category">Catergory: 
            @foreach (var category in product.Categories)
            {                    
                @if (product.Categories.LastOrDefault(category) != category)
                {
                    <span>@category, </span>
                }
                else
                {
                    <span>@category</span>
                }
            }
        </p>
    </div>
    <div>
        <p class="product-description">@product.Description</p>
    </div>
</section>